<!DOCTYPE html>
<html lang="en">
<head>
  <me
ta charset="UTF-8" />
  <meta name="viewport"
 content="width=device-width, initial-scale=1
.0"/>
  <title>Invoice Generator</title>
  <s
cript src="https://cdnjs.cloudflare.com/ajax/
libs/html2pdf.js/0.10.1/html2pdf.bundle.min.j
s"></script>
  <style>
    body {
      font-
family: Arial, sans-serif;
      margin: 0;
 
     padding: 2rem;
      background: #f8f9fa
;
    }
    .container-wrapper { /* To center
 the main container */
        display: flex;

        justify-content: center;
    }
    .
invoice-container {
      max-width: 800px;
 
     width: 100%;
      background: #fff;
   
   padding: 2rem;
      border-radius: 10px;

      box-shadow: 0 0 10px rgba(0,0,0,0.05);

    }
    h1 {
      text-align: center;
    
  margin-bottom: 1.5rem;
      color: #333;
 
   }

    .section {
        margin-bottom: 1
.5rem;
        padding-bottom: 1rem;
        
border-bottom: 1px solid #eee;
    }
    .sec
tion:last-child {
        border-bottom: none
;
        margin-bottom: 0;
    }
    .sectio
n h3 {
        margin-top: 0;
        margin-
bottom: 0.75rem;
        font-size: 1.1em;
  
      color: #555;
    }

    .grid-2-col {
 
       display: grid;
        grid-template-c
olumns: repeat(auto-fit, minmax(250px, 1fr));
 /* Responsive columns */
        gap: 1.5rem
;
    }

    label {
        display: block;

        margin-bottom: 0.25rem;
        font-
weight: 500;
        font-size: 0.9em;
    }

    input[type="text"],
    input[type="numbe
r"],
    input[type="url"],
    input[type="f
ile"],
    select,
    textarea {
      width
: 100%;
      padding: 8px;
      margin-bott
om: 0.75rem; /* Space below each input */
   
   border: 1px solid #ccc;
      border-radiu
s: 5px;
      box-sizing: border-box;
    }
 
   textarea {
        min-height: 70px;
     
   resize: vertical;
    }
    input[type="fi
le"] {
        padding: 5px; /* Slightly diff
erent padding for file input */
    }

    .l
ogo-preview-container {
        margin-top: 0
.5rem;
    }
    #logoPreview {
        max-w
idth: 150px;
        max-height: 80px;
      
  border: 1px solid #ddd;
        padding: 5p
x;
        border-radius: 4px;
        displa
y: none; /* Hidden by default */
    }
    #c
ompanyUrlDisplay { /* To display the URL as t
ext */
        font-size: 0.9em;
        colo
r: #007bff;
        word-break: break-all;
  
      margin-top: 0.5em;
    }
    #companyUr
lDisplay a {
        color: #007bff;
        
text-decoration: none;
    }
     #companyUrl
Display a:hover {
        text-decoration: un
derline;
    }


    table {
      width: 100
%;
      border-collapse: collapse;
      mar
gin-top: 1rem;
    }
    th, td {
      borde
r: 1px solid #ddd;
      padding: 10px;
     
 text-align: left;
    }
    td input[type="n
umber"] {
        text-align: right;
    }
  
  .lineTotal, .totals-value {
        text-al
ign: right;
    }
    th {
      background: 
#f1f1f1;
    }
    .totals-section {
      te
xt-align: right;
      margin-top: 1.5rem;
  
  }
    .totals-section p, .totals-section h3
 {
        margin: 0.5rem 0;
    }
    .total
s-section label {
        display: inline-blo
ck; /* For side-by-side layout */
        mar
gin-right: 0.5rem;
        text-align: left;

    }
     .totals-section input[type="number
"]{
        width: 80px; /* Smaller width for
 tax input */
        display: inline-block;

        text-align: right;
    }

    .btn-gr
oup {
        text-align: center;
        mar
gin-top: 2rem;
        padding-top: 1rem;
   
     border-top: 1px solid #eee;
    }
    .b
tn {
      padding: 10px 20px;
      backgrou
nd: #007bff;
      color: white;
      border
: none;
      border-radius: 5px;
      margi
n: 5px;
      cursor: pointer;
      font-siz
e: 0.95em;
    }
    .btn:hover {
      backg
round: #0056b3;
    }
    .remove-btn {
     
   background-color: #dc3545;
        color: 
white;
        padding: 5px 10px;
        fon
t-size: 0.8em;
    }
    .remove-btn:hover {

        background-color: #c82333;
    }

   
 /* Styles for the printable invoice (used by
 html2pdf) */
    #printableInvoiceHeader {
 
       display: flex;
        justify-content
: space-between;
        align-items: flex-st
art;
        margin-bottom: 20px; /* PDF marg
in */
        padding-bottom: 10px;
        b
order-bottom: 1px solid #ddd;
    }
    #prin
tableLogo {
        max-width: 150px;
       
 max-height: 70px;
    }
    #printableCompan
yDetails {
        text-align: right;
       
 font-size: 0.9em; /* Slightly smaller for PD
F */
    }
    #printableCompanyDetails stron
g {
        display: block;
        font-size
: 1.1em; /* Larger for company name */
      
  margin-bottom: 5px;
    }
     #printableCo
mpanyUrl {
        font-size: 0.8em;
        
color: #337ab7; /* Standard link blue for PDF
 */
    }
  </style>
</head>
<body>
<div clas
s="container-wrapper">
  <div class="invoice-
container">
    <h1>Invoice Generator</h1>

 
   <!-- Section for Company Details -->
    <
div class="section">
        <h3>Your Company
</h3>
        <div class="grid-2-col">
      
      <div>
                <label for="yourN
ame">Business Name:</label>
                <
input type="text" id="yourName" placeholder="
Your Business Name" oninput="saveInvoice();"/
>

                <label for="yourAddress">A
ddress:</label>
                <textarea id=
"yourAddress" placeholder="Example: 123 Main 
St, Anytown, ST 12345" oninput="saveInvoice()
;"></textarea>
            </div>
           
 <div>
                <label for="companyUrl
">Company Website/URL (optional):</label>
   
             <input type="url" id="companyUrl
" placeholder="https://www.example.com" oninp
ut="updateCompanyUrlDisplay(); saveInvoice();
" />
                <div id="companyUrlDispl
ay"></div>


                <label for="logo
Upload">Upload Logo (optional):</label>
     
           <input type="file" id="logoUpload"
 accept="image/png, image/jpeg, image/gif" on
change="previewLogo(); saveInvoice();"/>
    
            <div class="logo-preview-containe
r">
                    <img id="logoPreview"
 src="#" alt="Logo Preview" />
              
  </div>
            </div>
        </div>
  
  </div>

    <!-- Section for Invoice Meta &
 Client Info -->
    <div class="section">
  
    <div class="grid-2-col">
        <div>
  
          <h3>Invoice Details</h3>
          
  <label for="invoiceNumber">Invoice #:</labe
l>
            <input type="text" id="invoice
Number" oninput="saveInvoice();"/>

         
   <label for="currency">Currency:</label>
  
          <select id="currency" onchange="upd
ateTotals(); saveInvoice();">
              <
option value="USD" selected>USD ($)</option>

              <option value="EUR">EUR (€)</
option>
              <option value="GBP">GBP
 (£)</option>
              <option value="C
AD">CAD (C$)</option>
              <option v
alue="AUD">AUD (A$)</option>
            </se
lect>
        </div>
        <div>
          
  <h3>Bill To</h3>
            <label for="cl
ientName">Client Name:</label>
            <i
nput type="text" id="clientName" placeholder=
"Client's Full Name" oninput="saveInvoice();"
/>

            <label for="clientAddress">Cl
ient Address:</label>
            <textarea i
d="clientAddress" placeholder="Example: 456 O
ak Ave, Otherville, ST 67890" oninput="saveIn
voice();"></textarea>
        </div>
      </
div>
    </div>

    <!-- Section for Items -
->
    <div class="section">
      <h3>Items<
/h3>
      <table id="itemsTable">
        <t
head>
          <tr>
            <th>Descript
ion</th>
            <th>Qty</th>
           
 <th>Unit Price</th>
            <th>Total</t
h>
            <th>Action</th>
          </tr
>
        </thead>
        <tbody></tbody>
  
    </table>
      <button class="btn" onclic
k="addItem()">Add Item</button>
    </div>

 
   <!-- Section for Totals -->
    <div class
="totals-section">
      <label for="taxRate"
>Tax (%):</label>
      <input type="number" 
id="taxRate" value="0" step="0.01" min="0" on
input="updateTotals(); saveInvoice();" />
   
   <p>Subtotal: <span id="subtotal" class="to
tals-value">0.00</span></p>
      <p>Tax: <sp
an id="taxAmount" class="totals-value">0.00</
span></p>
      <h3>Total: <span id="total" c
lass="totals-value">0.00</span></h3>
    </di
v>

    <div class="btn-group">
        <butt
on class="btn" onclick="generatePDF()">Downlo
ad PDF</button>
    </div>
  </div>
</div>

 
 <script>
    const currencySymbols = { USD: 
"$", EUR: "€", GBP: "£", CAD: "C$", AUD: "
A$" };
    const logoUploadInput = document.g
etElementById('logoUpload');
    const logoPr
eviewImg = document.getElementById('logoPrevi
ew');
    const companyUrlInput = document.ge
tElementById('companyUrl');
    const company
UrlDisplay = document.getElementById('company
UrlDisplay');
    let logoBase64 = null; // T
o store the uploaded logo as Base64

    func
tion updateCompanyUrlDisplay() {
        cons
t url = companyUrlInput.value.trim();
       
 if (url) {
            let displayUrl = url;

            let fullUrl = url;
            i
f (!url.startsWith('http://') && !url.startsW
ith('https://')) {
                fullUrl = 
'http://' + url;
            }
            co
mpanyUrlDisplay.innerHTML = `Website: <a href
="${fullUrl}" target="_blank" rel="noopener n
oreferrer">${displayUrl}</a>`;
        } else
 {
            companyUrlDisplay.innerHTML = 
'';
        }
    }

    function previewLogo
() {
        const file = logoUploadInput.fil
es[0];
        if (file) {
            const 
reader = new FileReader();
            reader
.onload = function(e) {
                logoP
reviewImg.src = e.target.result;
            
    logoBase64 = e.target.result;
           
     logoPreviewImg.style.display = 'block';

            }
            reader.readAsDataUR
L(file);
        } else {
            logoPre
viewImg.src = '#';
            logoBase64 = n
ull;
            logoPreviewImg.style.display
 = 'none';
        }
    }

    function addI
tem(item = {}) {
      const table = document
.querySelector("#itemsTable tbody");
      co
nst row = document.createElement("tr");
     
 row.innerHTML = `
        <td><input type="t
ext" placeholder="Item description" value="${
item.desc || ""}" oninput="updateTotals(); sa
veInvoice();" /></td>
        <td><input type
="number" value="${item.qty || 1}" min="0" on
input="updateTotals(); saveInvoice();" /></td
>
        <td><input type="number" value="${i
tem.price || 0}" step="0.01" min="0" oninput=
"updateTotals(); saveInvoice();" /></td>
    
    <td class="lineTotal">0.00</td>
        <
td><button class="remove-btn" onclick="this.c
losest('tr').remove(); updateTotals(); saveIn
voice();">X</button></td>
      `;
      tabl
e.appendChild(row);
      updateTotals();
   
 }

    function updateTotals() {
      const
 rows = document.querySelectorAll("#itemsTabl
e tbody tr");
      let subtotal = 0;
      r
ows.forEach(row => {
        const qty = pars
eFloat(row.children[1].firstElementChild.valu
e) || 0;
        const price = parseFloat(row
.children[2].firstElementChild.value) || 0;
 
       const lineTotal = qty * price;
       
 row.querySelector(".lineTotal").textContent 
= formatCurrency(lineTotal);
        subtotal
 += lineTotal;
      });
      const taxRate 
= parseFloat(document.getElementById("taxRate
").value) || 0;
      const taxAmount = subto
tal * (taxRate / 100);
      const total = su
btotal + taxAmount;
      document.getElement
ById("subtotal").textContent = formatCurrency
(subtotal);
      document.getElementById("ta
xAmount").textContent = formatCurrency(taxAmo
unt);
      document.getElementById("total").
textContent = formatCurrency(total);
    }

 
   function formatCurrency(value) {
      con
st currency = document.getElementById("curren
cy").value;
      const numericValue = Number
(value);
      return `${currencySymbols[curr
ency]}${isNaN(numericValue) ? '0.00' : numeri
cValue.toFixed(2)}`;
    }

    function save
Invoice() {
      const itemsData = [];
     
 document.querySelectorAll("#itemsTable tbody
 tr").forEach(row => {
        const desc = r
ow.children[0].firstElementChild.value;
     
   const qty = row.children[1].firstElementCh
ild.value;
        const price = row.children
[2].firstElementChild.value;
        if (desc
.trim() || qty || price) itemsData.push({ des
c, qty, price });
      });

      const invo
iceData = {
        invoiceNumber: document.g
etElementById("invoiceNumber").value,
       
 currency: document.getElementById("currency"
).value,
        companyUrl: companyUrlInput.
value,
        logoBase64: logoBase64,
      
  yourName: document.getElementById("yourName
").value,
        yourAddress: document.getEl
ementById("yourAddress").value,
        clien
tName: document.getElementById("clientName").
value,
        clientAddress: document.getEle
mentById("clientAddress").value,
        taxR
ate: document.getElementById("taxRate").value
,
        items: itemsData
      };
      loc
alStorage.setItem("invoiceData", JSON.stringi
fy(invoiceData));
    }

    function loadInv
oice() {
      const saved = localStorage.get
Item("invoiceData");
      if (!saved) {
    
    document.getElementById("invoiceNumber").
value = generateInvoiceNumber();
        addI
tem();
        updateCompanyUrlDisplay();
   
     previewLogo();
        return;
      }
 
     const data = JSON.parse(saved);
      do
cument.getElementById("invoiceNumber").value 
= data.invoiceNumber || generateInvoiceNumber
();
      document.getElementById("currency")
.value = data.currency || "USD";
      compan
yUrlInput.value = data.companyUrl || "";
    
  updateCompanyUrlDisplay();

      if (data.
logoBase64) {
          logoBase64 = data.log
oBase64;
          logoPreviewImg.src = logoB
ase64;
          logoPreviewImg.style.display
 = 'block';
      } else {
          logoBase
64 = null;
          logoPreviewImg.style.dis
play = 'none';
      }

      document.getEle
mentById("yourName").value = data.yourName ||
 "";
      document.getElementById("yourAddre
ss").value = data.yourAddress || "";
      do
cument.getElementById("clientName").value = d
ata.clientName || "";
      document.getEleme
ntById("clientAddress").value = data.clientAd
dress || "";
      document.getElementById("t
axRate").value = data.taxRate || "0";
      d
ocument.querySelector("#itemsTable tbody").in
nerHTML = "";
      if (data.items && data.it
ems.length) {
        data.items.forEach(item
 => addItem(item));
      } else {
        ad
dItem();
      }
      updateTotals();
    }


    function generateInvoiceNumber() {
     
 const currentYear = new Date().getFullYear()
;
      let lastNum = parseInt(localStorage.g
etItem(`lastInvoiceNum_${currentYear}`) || "0
", 10);
      lastNum++;
      localStorage.s
etItem(`lastInvoiceNum_${currentYear}`, lastN
um);
      return `${currentYear}-${String(la
stNum).padStart(4, '0')}`;
    }

    functio
n generatePDF() {
        const yourName = do
cument.getElementById("yourName").value || "Y
our Company";
        const yourAddress = doc
ument.getElementById("yourAddress").value.rep
lace(/\n/g, '<br>') || "Your Address";
      
  let companyUrl = companyUrlInput.value.trim
();
        let companyLink = companyUrl;
   
     if (companyUrl && !companyUrl.startsWith
('http://') && !companyUrl.startsWith('https:
//')) {
            companyLink = 'http://' +
 companyUrl;
        }


        let pdfHeade
rHtml = `
            <div id="printableInvoi
ceHeader">
                <div style="flex: 
1; text-align: left;">`;
        if (logoBase
64) {
            pdfHeaderHtml += `<img id="
printableLogo" src="${logoBase64}" alt="Compa
ny Logo" style="vertical-align: top;">`;
    
    }
        pdfHeaderHtml += `</div>
      
          <div id="printableCompanyDetails" s
tyle="flex: 2;">
                    <strong>
${yourName}</strong>
                    ${yo
urAddress}`;
        if (companyUrl) {
      
      pdfHeaderHtml += `<br><a href="${compan
yLink}" target="_blank" id="printableCompanyU
rl">${companyUrl}</a>`;
        }
        pdf
HeaderHtml += `</div>
            </div>
    
        <hr style="margin: 10px 0 20px 0;">`;



        const elementToPrint = document.cr
eateElement('div');

        let invoiceDetai
lsHtml = '<div style="display: flex; justify-
content: space-between; margin-bottom: 20px;"
>';
        invoiceDetailsHtml += '<div style
="flex: 1;"><h3>Invoice Details</h3>';
      
  invoiceDetailsHtml += `<p><strong>Invoice #
:</strong> ${document.getElementById("invoice
Number").value || '-'}</p>`;
        invoiceD
etailsHtml += `<p><strong>Currency:</strong> 
${document.getElementById("currency").options
[document.getElementById("currency").selected
Index].text}</p></div>`;

        invoiceDeta
ilsHtml += '<div style="flex: 1; text-align: 
right;"><h3>Bill To</h3>';
        invoiceDet
ailsHtml += `<p><strong>${document.getElement
ById("clientName").value || '-'}</strong></p>
`;
        invoiceDetailsHtml += `<p>${docume
nt.getElementById("clientAddress").value.repl
ace(/\n/g, '<br>') || '-'}</p></div>`;
      
  invoiceDetailsHtml += '</div>';
        ele
mentToPrint.innerHTML += invoiceDetailsHtml;


        let itemsTableHtml = '<h3>Items</h3>
<table style="width:100%; border-collapse: co
llapse; font-size:0.9em;"><thead><tr>';
     
   const headers = ["Description", "Qty", "Un
it Price", "Total"];
        headers.forEach(
header => itemsTableHtml += `<th style="borde
r: 1px solid #ddd; padding: 8px; background: 
#f1f1f1; text-align:left;">${header}</th>`);

        itemsTableHtml += '</tr></thead><tbod
y>';

        document.querySelectorAll("#ite
msTable tbody tr").forEach(row => {
         
   itemsTableHtml += '<tr>';
            item
sTableHtml += `<td style="border: 1px solid #
ddd; padding: 8px;">${row.children[0].querySe
lector('input').value || '-'}</td>`;
        
    itemsTableHtml += `<td style="border: 1px
 solid #ddd; padding: 8px; text-align:right;"
>${row.children[1].querySelector('input').val
ue || '0'}</td>`;
            itemsTableHtml 
+= `<td style="border: 1px solid #ddd; paddin
g: 8px; text-align:right;">${formatCurrency(p
arseFloat(row.children[2].querySelector('inpu
t').value || 0))}</td>`;
            itemsTab
leHtml += `<td style="border: 1px solid #ddd;
 padding: 8px; text-align:right;">${row.query
Selector(".lineTotal").textContent}</td>`;
  
          itemsTableHtml += '</tr>';
        
});
        itemsTableHtml += '</tbody></tabl
e>';
        elementToPrint.innerHTML += item
sTableHtml;

        let totalsHtml = '<div s
tyle="text-align: right; margin-top: 20px; fo
nt-size:0.9em;">';
        totalsHtml += `<p>
<strong>Subtotal:</strong> ${document.getElem
entById("subtotal").textContent}</p>`;
      
  totalsHtml += `<p><strong>Tax (${document.g
etElementById("taxRate").value || 0}%):</stro
ng> ${document.getElementById("taxAmount").te
xtContent}</p>`;
        totalsHtml += `<h3 s
tyle="font-size:1.1em;"><strong>Total:</stron
g> ${document.getElementById("total").textCon
tent}</h3>`;
        totalsHtml += '</div>';

        elementToPrint.innerHTML += totalsHtm
l;

        const finalHtmlForPdf = `<div>${p
dfHeaderHtml}${elementToPrint.innerHTML}</div
>`;

        const invoiceNumberValue = docum
ent.getElementById("invoiceNumber").value || 
"invoice";
        const opt = {
            
margin:       [0.5, 0.5, 0.5, 0.5],
         
   filename:     `invoice-${invoiceNumberValu
e}.pdf`,
            image:        { type: 'j
peg', quality: 0.98 },
            html2canva
s:  { scale: 2, logging: false, useCORS: true
 },
            jsPDF:        { unit: 'in', f
ormat: 'letter', orientation: 'portrait' }
  
      };

        html2pdf().from(finalHtmlFo
rPdf).set(opt).save()
            .catch(err 
=> console.error("PDF generation error:", err
));
    }

    window.onload = () => {
      
loadInvoice();
    };
  </script>
</body>
</h
tml>


