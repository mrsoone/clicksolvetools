<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tommy's Coin Flip Game</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            background-color: #282c34;
            color: white;
            padding: 20px;
            transition: background 0.5s ease-in-out;
        }
        h1 {
            font-size: 36px;
            text-shadow: 2px 2px 10px rgba(255, 255, 255, 0.8);
        }
        button {
            font-size: 22px;
            padding: 15px 30px;
            margin: 20px;
            background: linear-gradient(45deg, #ff0000, #ff7300);
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            box-shadow: 0 0 10px rgba(255, 0, 0, 0.7);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        button:hover {
            transform: scale(1.1);
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
        }
        .coin-container {
            display: flex;
            justify-content: center;
            margin: 20px;
        }
        .coin {
            font-size: 100px;
            transition: transform 0.5s ease-in-out;
        }
        .flipping {
            animation: flip 0.5s infinite;
        }
        @keyframes flip {
            0% { transform: rotateY(0deg); }
            100% { transform: rotateY(360deg); }
        }
    </style>
</head>
<body>
    <h1>🪙 Tommy's Coin Flip Game 🪙</h1>
    <p>Click the button to flip the coin!</p>
    <div class="coin-container">
        <div id="coin" class="coin">😀</div>
    </div>
    <button onclick="flipCoin()">Flip Coin</button>
    <p id="result">Result: ?</p>

    <script>
        function flipCoin() {
            let coin = document.getElementById("coin");
            let result = document.getElementById("result");
            
            // Add flipping animation
            coin.classList.add("flipping");
            result.innerText = "Flipping...";
            
            setTimeout(() => {
                coin.classList.remove("flipping");
                let coinValue = Math.random() < 0.5 ? "Heads" : "Tails";
                let coinFaces = { "Heads": "😀", "Tails": "🪙" };
                coin.innerText = coinFaces[coinValue];
                result.innerText = `Result: ${coinValue}`;
            }, 1000);
        }
    </script>
</body>
</html>
